'use client';

import { HeroSection } from "@/components/hero-section/hero-section";

export default function Home() {
  return (
    <main>
      <HeroSection />
      {/* Future: Sections for product categories, etc. */}
    </main>
  );
}